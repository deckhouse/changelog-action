import { formatYaml, formatMarkdown } from "./format"

describe("YAML", () => {
	test.todo("returns non-empty output")
	test.todo("includes UNKNOWN on the top")
})

describe("Markdown", () => {
	test.todo("has milestone header")
	test.todo("shows modules as sub-headers")
})
